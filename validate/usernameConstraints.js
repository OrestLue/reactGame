export const usernameConstraints = {
    username: {
        presence: {
            allowEmpty: false,
            message: '^Please enter a Full Name'
        },
    },
};

export default usernameConstraints;